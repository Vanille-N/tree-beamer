== Practice talk 2024-03-29



=== Remember

Quite fast. Leave a bit more time for the examples.

"sub object provenance". ban "OOB".

clarify which parts of the design are new vs same



=== Modify


fix layout issue slide 4
[DONE]

Slide 12
- don't go into so much detail of the state machine
- prefer motivating with an example

Slides 3,4 are bad.
- go through step by step (the code, and the optimization)
- high level explanation of what the client does wrong
- we can inline while keeping the function structure
- don't show the optimized version at that point
(slide 4, opaque inlined, main appears later, optimized version absent, high-level point (it's reading a thing it shouldn't read))
[DONE]


Slide 14
- don't hide the code. Grey it out ? Highlight the current line with an arrow.
- start with presentation of the visual format
- "Retag" not introduced. Replace with "borrow" ? Put the same text on the arrow that highlights the code.
- "noop" -> "unchanged" ?
- "
  old: Reserved
  child write
  Active
  " ?
- consider unabbreviating the state machine
- [Rejected] is weird. Write "UB" next to the state.

Slide 7
- remove mention of dynamic, we don't do restriction of the range
[DONE]

Slide 16
Merge read-only with 2-phase.

Slide 18
Don't show all the variants.
Only one version.
If we want to show another rejected example, Johannes' optimization is available, but it needs time.

After each example, main point that we learned
- "raw pointers inherit permissions of the parent, that's how we permit aliasing"
- etc

Mention protectors by name.





=== Add

talk about the evaluation + ongoing work
[DONE]

explain what SB fails to handle
[DONE]

SB _doesn't_ handle 2-phase, it just gives up.
This is the opportunity to actually handle them properly, and it turn out to have
extra benefits.
=> contribution slide
[DONE]

after SB is introduced, one slide for its limitations. After the first example ?
briefly mention what is inherited ? We definitely don't want to explain how SB works.
[DONE]

Example of raw pointer being rejected by SB
  ```rs
  let mut x = 0;
  let r = addr_of_mut!(x);
  *x = 42;
  r.write(50)
  ```
This should REPLACE the example with interior mutability
After the raw pointer example, mention that
- we do the same thing for interior mutable
- maybe this accepts too much, looking for a middle ground in the future

After 1st example, why does it have to be a tree ? -> 2nd example that SB rejects (slice.as_mut_ptr ?)
Alternative: before we present the tree structure, show insufficiency of SB to motivate the tree
Structure of provenance is a tree, SB was just choosing an approximation that turns out to be too restrictive.
TB removes that simplification, which now supports Reserved.

2-phase example is only 2-phase-like.
Motivation: there is unsafe code that follows this pattern that existed in the wild
before SB either
- ruled them out (as_mut_ptr example)
- introduced hacks to accept them
TB accepts them all without hacks.

